const test = require('tape')

test('Day 8.A - Code string length vs string length', t => {
  t.comment('Failed to come up with unit tests')
  t.end()
})

test('Day 8.B - ', t => {
  t.comment('Failed to come up with unit tests')
  t.end()
})
